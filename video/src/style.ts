/**
 * Re-export de tokens + familias de fuente resueltas, para que los componentes
 * importen todo desde un único lugar.
 */
export * from "./theme";
import { FONT_FAMILY } from "./fonts";

export const FONT_FAMILY_SANS = FONT_FAMILY.sans;
export const FONT_FAMILY_SERIF = FONT_FAMILY.serif;
